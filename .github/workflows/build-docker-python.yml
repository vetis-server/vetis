name: Build Docker Image (Python)

permissions:
  contents: read
  
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "**" ]
    # paths-ignore:
    #   - ".github/workflows/**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6

    - name: Build the Docker image (Python)
      run: |
        cd vetis
        docker build . --file Dockerfile.python --tag vetis-python:$(date +%s)